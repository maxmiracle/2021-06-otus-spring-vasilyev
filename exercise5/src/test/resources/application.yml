spring:
  datasource:
    url: jdbc:h2:mem:bookshelf
    username: sa
    password:
    driver-class-name: org.h2.Driver
    data: data.sql
    schema: schema.sql
  shell:
    interactive:
      enabled: false